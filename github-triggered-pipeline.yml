# Stub pipeline - lives in or is pointed at your source code repo
trigger:
  branches:
    include:
      - main

pool:
  vmImage: 'ubuntu-latest'

steps:
  - script: echo "Source repo commit detected - triggering downstream pipeline"
    displayName: 'Stub trigger'
